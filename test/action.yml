name: "test action"
description: "runs gotest"

runs:
  using: composite
  steps:
    - uses: gcjuarez/crashcourse-githubactions-playground/deps@v2
    
    - name: run gotest
      shell: bash
      run: go test ./...