name: Go CI checks
on:
  push:
    branches:
      - "**"
    tags-ignore:
      - "**"

jobs:
  go-workflow:
    uses: gcjuarez/crashcourse-reusable-workflows/.github/workflows/main.yml@main
    with:
      project-name: crashcourse-gera
      commands: '["test","lint"]'
